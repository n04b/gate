.DS_Store

.env

node_modules/
dist/
coverage/

# JWT key material never belongs in the repository.
keys/
*.pem

# Live configuration: generated on first container start, edited per host.
# config/gate.example.yaml is the reference kept in the repository.
config/gate.yaml
config/gate.dev.yaml
